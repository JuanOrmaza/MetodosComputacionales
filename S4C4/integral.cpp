#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    float inicio;
    float fin;
    inicio=0;
    fin=(3*PI)/2
    int lista[100];
    for(int i=1; i<=a; i++){
        f=f*i;
    }
    return 0;
}



/*
#El intervalo de integracion es de 0 a 3pi/2. Divida el intervalo de integracion en M secciones para calcular sus integrales.
a=0
b=3*np.pi/2
n=101
h=(b-a)/(n-1)

# 1a). Usando el metodo de suma de rectangulos, calcule la integral de la funcion. Compare su valor obtenido numericamente con el valor analitico e imprima ambos valores.
suma=0
for i in range(0,n+1):
    paso=a+i*h
    suma+=h*funcion(paso)
print("la integral por suma de rectangulos es: ",suma, ' mientras que el valor analitico es igual a -1')


*/